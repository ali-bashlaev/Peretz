<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:orientation="vertical"
    tools:background="@color/brand_cod_gray">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/home_category_card"
        android:layout_width="72dp"
        android:layout_height="72dp"
        app:cardBackgroundColor="@color/home_category_background"
        app:cardCornerRadius="50dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/home_category_icon"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            tools:src="@drawable/ic_burger" />
    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/home_category_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="8dp"
        android:textColor="@color/home_category_text"
        android:textSize="12sp"
        tools:text="Роллы" />

</LinearLayout>